{
  "name": "@sli/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "start": "node dist/main.js",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "bootstrap:admin": "node dist/scripts/bootstrap-admin.js",
    "test:file-parser": "pnpm build && node dist/staging/file-parser.local-test.js",
    "test:widget-snapshot": "pnpm build && node dist/shared/widget-result-snapshot.local-test.js",
    "test:general-chat-tools": "rm -rf dist && pnpm exec tsc -p tsconfig.build.json --incremental false && node dist/agents/general-chat-tools.local-test.js",
    "test:occurrences": "rm -rf dist && pnpm exec tsc -p tsconfig.build.json --incremental false && node dist/occurrences/occurrences.local-test.js",
    "test:inbox": "rm -rf dist && pnpm exec tsc -p tsconfig.build.json --incremental false && node dist/inbox/inbox.local-test.js",
    "test:attendance-agent": "rm -rf dist && pnpm exec tsc -p tsconfig.build.json --incremental false && node dist/agents/attendance-agent.local-test.js",
    "test:agents": "rm -rf dist && pnpm exec tsc -p tsconfig.build.json --incremental false && node dist/agents/agents.local-test.js",
    "test:public-chat": "rm -rf dist && pnpm exec tsc -p tsconfig.build.json --incremental false && node dist/public-chat/public-chat.local-test.js"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^10.4.15",
    "@nestjs/platform-express": "^10.4.15",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.9",
    "@nestjs/schematics": "^10.2.3",
    "@types/express": "^5.0.0",
    "typescript": "^5.8.3"
  }
}
