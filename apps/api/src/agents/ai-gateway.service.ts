import { Injectable } from '@nestjs/common';
@Injectable() export class AiGatewayService { async runAgent(input:{tenantId?:string;agent:Record<string,unknown>;runType?:string;triggerType?:string;inputSnapshot:Record<string,unknown>;messages?:Array<{role:string;content:string}>;responseSchema?:Record<string,unknown>}) { const enabled=process.env.AI_GATEWAY_ENABLED==='true', dry=!enabled||process.env.AI_GATEWAY_DRY_RUN==='true'; const model=String(input.agent.model_name||process.env.OPENAI_DEFAULT_MODEL||'gpt-4.1-mini'); if(dry)return {output_json:this.simulation(input),usage:{input_tokens:0,output_tokens:0,total_tokens:0},model_provider:'openai',model_name:model,dry_run:true}; if(!process.env.OPENAI_API_KEY) throw new Error('AI Gateway habilitado sem chave configurada.'); const system='Responda somente em pt-BR, de forma executiva, usando exclusivamente o snapshot fornecido. Nunca calcule indicadores, invente dados, consulte fontes externas, SQL, banco ou dados crus. Devolva JSON válido conforme o schema.'; const response=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model,input:[{role:'system',content:system},{role:'user',content:JSON.stringify({snapshot:input.inputSnapshot,messages:input.messages??[]})}],temperature:Number(input.agent.temperature??.2),max_output_tokens:Number(input.agent.max_output_tokens??1200),text:{format:{type:'json_object'}}})}); if(!response.ok)throw new Error('Falha na chamada do provedor de IA.'); const data=await response.json() as any; const text=data.output_text||data.output?.flatMap((x:any)=>x.content??[]).map((x:any)=>x.text).join(''); let output_json:unknown;try{output_json=JSON.parse(text)}catch{throw new Error('O provedor retornou JSON inválido.')} const usage=data.usage??{};return {output_json,usage:{input_tokens:usage.input_tokens??0,output_tokens:usage.output_tokens??0,total_tokens:usage.total_tokens??0},model_provider:'openai',model_name:model,dry_run:false}; } private simulation(input:any){const widgets=input.inputSnapshot?.widgets??[];if(input.messages?.length)return {answer:'Esta resposta considera somente os widgets e filtros atualmente apresentados neste dashboard.',related_widgets:widgets.slice(0,3).map((w:any)=>w.id),limitations:['A análise não calcula novos indicadores e depende dos dados exibidos.']};return {opening:'Análise simulada do dashboard concluída.',executive_summary:'Os resultados abaixo refletem somente os widgets e filtros atuais.',highlights:widgets.length?[`${widgets.length} widget(s) foram considerados na análise.`]:['Não há widgets suficientes para análise.'],attention_points:[],risks:[],recommendations:['Acompanhe os pontos destacados nos widgets do dashboard.'],widget_insights:widgets.map((w:any)=>({widget_id:w.id,widget_title:w.title,analysis:'Widget incluído no snapshot tratado; avalie o resultado exibido.'})),data_quality_notes:widgets.length?[]:['Dados insuficientes para uma análise detalhada.']};} }
